import MessageItem from 'src/components/MessageItem';

function ChatPage() {
  // Renderreturn (
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
      }}
    >
      <MessageItem />
    </div>
  );
}
export default ChatPage;
